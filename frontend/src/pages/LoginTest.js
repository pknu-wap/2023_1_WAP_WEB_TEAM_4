import React, { useState } from "react";

function LoginTest() {
  const [repeat, setRepeat] = useState([]);

  return (
    <div>
      {repeat.map((word, i) => {
        {
        }
      })}
    </div>
  );
}

export default LoginTest;
